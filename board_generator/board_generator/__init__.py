"""board_generator - fixed-bank Codenames Duet board generator.

Standalone, isolated tool that produces a fixed bank of T = 30 boards instrumented to measure gender
bias in LLMs. The platform only reads the emitted board files; this package never imports from (or
is imported by) the platform's backend/.
"""

__version__ = "0.1.0"
